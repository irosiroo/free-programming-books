(() => { const root=document.documentElement; const saved=localStorage.getItem('fpb-theme'); if(saved) root.dataset.theme=saved; const b=document.querySelector('[data-theme-toggle]'); if(b)b.addEventListener('click',()=>{const next=root.dataset.theme==='dark'?'light':'dark';root.dataset.theme=next;localStorage.setItem('fpb-theme',next)}); const form=document.querySelector('[data-search]'); if(form) form.addEventListener('submit',e=>{e.preventDefault();const q=new FormData(form).get('q')?.trim();if(q) location.href=`https://ebookfoundation.github.io/free-programming-books-search/?search=${encodeURIComponent(q)}`}); })();

